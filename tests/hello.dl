void main() {
    // Define a simple array
    int[] numbers = [1, 2, 3, 4, 5];

    // Print the array
    writeln("Array: ", numbers);

    // Access elements
    writeln("First element: ", numbers[0]);
    writeln("Last element: ", numbers[$ - 1]);

    
    writeln("Modified Array: ", numbers);

    // Iterate over the array
    writeln("Array elements:");
    int num;
    num = 0;
    foreach (num; numbers) {
        writeln(num);
    }
}