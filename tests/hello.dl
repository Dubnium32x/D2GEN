import std.stdio;

struct Vec2 {
    int x;
    int y;
};

void main() {
    Vec2 pos;
    pos.x = 12;
    pos.y = 34;

    writeln("Position: (", pos.x, ", ", pos.y, ")");

    // Struct assignment
    Vec2 other;
    other = pos;
    writeln("Other: (", other.x, ", ", other.y, ")");
}